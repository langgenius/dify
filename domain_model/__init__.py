"""Standalone Human Input v2 domain model stubs.

This package intentionally has no dependency on API controllers, ORM models, or
workflow runtime implementations. It exists to make domain boundaries and
relationships reviewable before persistence and application services are added.
"""

{
  "name": "dify",
  "private": true,
  "scripts": {
    "prepare": "command -v git >/dev/null 2>&1 || exit 0; git rev-parse --git-dir >/dev/null 2>&1 || exit 0; vp config"
  },
  "devDependencies": {
    "taze": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": "^22.22.1"
  },
  "packageManager": "pnpm@10.33.0"
}

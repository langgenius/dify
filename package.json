{
  "name": "dify",
  "private": true,
  "scripts": {
    "prepare": "node -e \"if (process.env.NODE_ENV !== 'production'){process.exit(1)} \" || vp config"
  },
  "devDependencies": {
    "taze": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": "^22.22.1"
  },
  "packageManager": "pnpm@10.33.0"
}

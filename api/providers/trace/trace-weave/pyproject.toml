[project]
name = "dify-trace-weave"
version = "0.0.1"
dependencies = [
    "weave>=0.52.36,<1.0.0",
]
description = "Dify ops tracing provider (Weave)."

[tool.setuptools.packages.find]
where = ["src"]

# Dify Gevent Bootstrap

This package exists only for gevent bootstrap helpers:

- gevent runtime guardrails
- gevent third-party compatibility patching

Do not add unrelated Python code here.

If code does not directly participate in gevent startup / patch verification,
it should live somewhere else.

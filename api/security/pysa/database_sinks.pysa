# SQLAlchemy raw-text construction and execution. Expression-language objects
# are not sinks; only strings reaching raw SQL boundaries are modeled.
def sqlalchemy.text(text: TaintSink[SQL]): ...
def sqlalchemy.engine.Connection.exec_driver_sql(self, statement: TaintSink[SQL], parameters, execution_options): ...

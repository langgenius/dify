"""add_workflow_alias_management

Revision ID: 398394623b7b
Revises: 532b3f888abf
Create Date: 2025-08-12 17:08:07.344942

"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql


# revision identifiers, used by Alembic.
revision = '398394623b7b'
down_revision = '532b3f888abf'
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###

    # Create workflow_aliases table
    op.create_table('workflow_aliases',
        sa.Column('id', postgresql.UUID(), server_default=sa.text('uuid_generate_v4()'), nullable=False),
        sa.Column('tenant_id', postgresql.UUID(), nullable=False),
        sa.Column('app_id', postgresql.UUID(), nullable=False),
        sa.Column('workflow_id', postgresql.UUID(), nullable=False),
        sa.Column('alias_name', sa.String(length=255), nullable=False),
        sa.Column('alias_type', sa.String(length=50), nullable=False, server_default='custom'),  # 'system' | 'custom'
        sa.Column('created_by', postgresql.UUID(), nullable=False),
        sa.Column('created_at', sa.DateTime(), server_default=sa.text('CURRENT_TIMESTAMP(0)'), nullable=False),
        sa.Column('updated_at', sa.DateTime(), server_default=sa.text('CURRENT_TIMESTAMP(0)'), nullable=False),
        sa.PrimaryKeyConstraint('id', name='workflow_alias_pkey')
    )

    # Create indexes for better query performance
    with op.batch_alter_table('workflow_aliases', schema=None) as batch_op:
        # Index for app-level alias uniqueness check
        batch_op.create_index('workflow_alias_app_name_idx', ['app_id', 'alias_name'], unique=True)
        # Index for workflow-level alias queries
        batch_op.create_index('workflow_alias_workflow_idx', ['tenant_id', 'app_id', 'workflow_id'], unique=False)
        # Index for tenant-level queries
        batch_op.create_index('workflow_alias_tenant_idx', ['tenant_id'], unique=False)
        # Index for alias type queries (system vs custom)
        batch_op.create_index('workflow_alias_type_idx', ['alias_type'], unique=False)

    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###

    # Drop workflow_aliases table and all its indexes
    op.drop_table('workflow_aliases')

    # ### end Alembic commands ###

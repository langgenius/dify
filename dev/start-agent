#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/../dify-agent"

exec uv run --project . --extra server --env-file .env \
  uvicorn dify_agent.server.app:app --host 127.0.0.1 --port 5050 --reload

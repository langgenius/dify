#!/bin/bash

set -x

SCRIPT_DIR="$(dirname "$(realpath "$0")")"
cd "$SCRIPT_DIR/../api"

# Avoid shell-level debug env vars overriding api/.env values.
unset DEBUG
unset FLASK_DEBUG

uv run flask db upgrade

uv run \
  flask run --host 0.0.0.0 --port=5001 --debug

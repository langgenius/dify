import * as React from 'react'
import EnterpriseMarketplace from '@/app/components/explore/enterprise-marketplace'

const MarketplacePage = () => {
  return <EnterpriseMarketplace />
}

export default React.memo(MarketplacePage)

'use client'

import { memo } from 'react'
import Workflow from '@/app/components/workflow'

const Page = () => {
  return (
    <Workflow
      nodes={[]}
      edges={[]}
    />
  )
}
export default memo(Page)

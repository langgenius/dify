export enum ChatVarType {
  Number = 'number',
  String = 'string',
  Secret = 'secret',
  Object = 'object',
  ArrayString = 'array[string]',
  ArrayNumber = 'array[number]',
  ArrayObject = 'array[object]',
}

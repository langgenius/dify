import type { BlockEnum } from '@/app/components/workflow/types'
import { memo } from 'react'

type HelpLinkProps = {
  nodeType: BlockEnum
}
const HelpLink = (_props: HelpLinkProps) => {
  return null
}

export default memo(HelpLink)

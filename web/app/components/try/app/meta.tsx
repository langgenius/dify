'use client'
import type { FC } from 'react'
import React from 'react'

type Props = {

}

const Meta: FC<Props> = ({
}) => {
  return (
    <div>
      Meta Info
    </div>
  )
}
export default React.memo(Meta)

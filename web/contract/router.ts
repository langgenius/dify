import { collectionPluginsContract, collectionsContract } from './marketplace'

export const marketPlaceRouterContract = {
  collections: collectionsContract,
  collectionPlugins: collectionPluginsContract,
}
